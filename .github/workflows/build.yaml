name: Build
run-name: ${{github.actor}} is conducting a build of maas-ctl
on:
  pull_request:
    types: [opened, synchronize]
  push:
    branches: [main]
    tags: ["v**"]
permissions:
  id-token: write
  contents: read
defaults:
  run:
    shell: bash

jobs:

  build:
    name: Build
    runs-on: [ubuntu-latest]

    steps:
      - uses: actions/checkout@v3

      - name: Configure AWS ECR Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: ${{ secrets.AWS_ECR_ROLE }}
          role-session-name: arkime-ecr
          aws-region: us-east-1

      - name: ECR Login
        uses: aws-actions/amazon-ecr-login@v1
        id: login-ecr

      - name: Build wheel file and container image
        run: |
          make _build_poetry
          make build_all

      - name: show images
        run: |
          docker images